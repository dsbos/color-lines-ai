package com.us.dsb.colorlines.game.lines

// ???? TODO: Maybe gather together multiple parameters (board order, line
//   order, numbers of initial and added balls, scoring.
//   Also, maybe have regular vs. reduced versions, with redirecting version
//    having single source-level switch between them (or runtime switch?).

/** Order (length) of lines. */
/*private?*/ type LineOrder = 5
/*private?*/ val LineOrder: LineOrder = valueOf[LineOrder]
