package com.us.dsb.colorlines.expl.nn

object TypesToSort {
  case class OneHiddenTopology(inputLayerSize : Int,
                               hiddenLayerSize: Int,
                               outputLayerSize: Int)
}
